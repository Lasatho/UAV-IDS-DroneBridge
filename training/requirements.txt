# Core
numpy>=1.24,<2.0
pandas>=2.0
torch>=2.1
pyyaml>=6.0

# Experiment tracking
wandb>=0.16
tensorboard>=2.15  # only needed if logging.backend == "tensorboard"

# Export (install when needed, not required for training)
# onnx>=1.15
# onnxruntime>=1.17